# 백엔드 설치 방법 (Supabase)

이 문서는 실제로 설치를 마친 순서 그대로입니다.
다른 고객사에 같은 구성을 올릴 때도 이대로 따라가면 됩니다.

현재 프로젝트

```
Project URL : https://izrtclsqhsgkuwsffifn.supabase.co
Publishable : sb_publishable_G5wx-YdgwUU630x1vKmZmQ_15BgxvEX
Region      : ap-northeast-2 (서울)
```

---

## 1. 프로젝트 만들기

[supabase.com](https://supabase.com) → **New project**

- **Region**: **Northeast Asia (Seoul)** — 고객 개인정보를 국내에 두는 편이 개인정보보호법상 간단합니다
- **Database Password**: 길게 만들어 **비밀번호 관리자에 저장**하세요. 다시 볼 수 없습니다
- **Security** 항목을 이렇게 맞춥니다

  | 항목 | 설정 | 이유 |
  |---|---|---|
  | Enable Data API | 켬 | 앱이 데이터베이스와 이야기하는 통로 |
  | Automatically expose new tables | **끔** | 어떤 표를 얼마나 열지 직접 정합니다 (Supabase 권장) |
  | Enable automatic RLS | **켬** | 나중에 표를 추가해도 접근제어가 자동으로 걸립니다 |

만들어지면 **Settings → API Keys** 에서 **Project URL** 과 **Publishable key** 를 복사합니다.

> Publishable 키는 앱에 그대로 넣는 공개 값입니다. 접근 제어는 키가 아니라
> 데이터베이스의 행 단위 규칙이 합니다.
> 같은 화면의 **Secret 키와 service_role 키는 절대 앱이나 깃허브에 넣지 마세요.**
> 그 키들은 모든 규칙을 무시합니다.

## 2. 표와 접근 규칙 만들기

**SQL Editor → New query** 에 `supabase/migrations/0001_init.sql` 을 통째로 붙여넣고 **Run**.

### 바로 검증하세요

새 쿼리에 `supabase/tests/rls-test.sql` 을 붙여넣고 **Run**.
결과에 **`모든 시험 통과 (12/12)`** 가 나와야 합니다.

회사가 둘, 사람이 넷인 상황을 만들어서 이런 것들을 확인합니다.

- 다른 회사 데이터가 보이는가
- 일반 사용자에게 남의 담당 고객이 보이는가
- 남의 회사 고객을 고칠 수 있는가
- 일반 사용자가 스스로 관리자가 될 수 있는가

하나라도 뚫리면 그 자리에서 멈추고 어디가 뚫렸는지 알려 줍니다.
시험 데이터는 끝나면 되돌아가 남지 않습니다.

## 3. 서버 함수 올리기

```bash
brew install supabase/tap/supabase
supabase login
```

`supabase login` 은 브라우저에서 승인하는 절차라 사람이 직접 해야 합니다.

```bash
cd <프로젝트 폴더>
supabase functions deploy read-card  --project-ref <프로젝트ref>
supabase functions deploy admin-user --no-verify-jwt --project-ref <프로젝트ref>
```

> `admin-user` 에 `--no-verify-jwt` 를 주는 이유
> 회사를 처음 개설할 때는 아직 계정이 없어 토큰이 없습니다.
> 그래서 플랫폼의 토큰 검사를 끄고, 함수가 **스스로** 확인합니다.
> 회사 개설은 설치 암호로, 나머지 기능은 호출자가 그 회사 관리자인지
> 직접 검사합니다. 검사를 안 하는 것이 아니라 함수 안으로 옮긴 것입니다.

### 비밀값 넣기

```bash
supabase secrets set SETUP_SECRET="$(openssl rand -base64 24)" --project-ref <ref>
supabase secrets set ANTHROPIC_API_KEY=sk-ant-... --project-ref <ref>
```

- `SETUP_SECRET` — 회사 개설에만 쓰는 암호. 이 저장소에서는 `.setup-secret` 파일에 두고 깃에서 제외했습니다
- `ANTHROPIC_API_KEY` — **이제 키는 서버에만 있습니다.** 각자 폰에 넣던 방식은 없어졌습니다

> 명령줄에 키를 직접 쓰면 셸 기록(`~/.zsh_history`)에 평문으로 남습니다.
> 신경 쓰이면 넣은 뒤 `sed -i '' '/ANTHROPIC_API_KEY/d' ~/.zsh_history` 로 지우세요.

### 배포되면 이렇게 확인합니다

```bash
B=https://<ref>.supabase.co/functions/v1
curl -s -o /dev/null -w "%{http_code}\n" -X POST "$B/read-card" -d '{"image":"x"}'      # 401 이어야 정상
curl -s -X POST "$B/admin-user" -d '{"action":"bootstrap","code":"TESTAA"}'              # 설치 암호 오류여야 정상
curl -s -X POST "$B/admin-user" -d '{"action":"create","loginId":"spy"}'                 # 로그인 필요여야 정상
```

## 4. 첫 회사와 관리자 계정

비밀번호가 화면과 셸 기록에 남지 않도록 이렇게 실행합니다.

```bash
read -s -p "관리자 비밀번호 (10자 이상): " PW; echo
curl -X POST "https://<ref>.supabase.co/functions/v1/admin-user" \
  -H "Content-Type: application/json" \
  -H "x-setup-secret: $(cat .setup-secret)" \
  -d "{\"action\":\"bootstrap\",\"code\":\"ACTIVA\",\"companyName\":\"액티바코리아\",\"loginId\":\"admin\",\"name\":\"관리자\",\"password\":\"$PW\"}"
unset PW
```

`{"ok":true}` 가 나오면 앱에서 **회사 코드 / 아이디 / 비밀번호**로 들어갑니다.

## 5. 마지막 잠그기 (운영 시작 전)

```bash
supabase secrets set ALLOWED_ORIGIN="https://motodna.github.io" --project-ref <ref>
```

설정하지 않으면 어느 사이트에서든 우리 함수를 부를 수 있습니다.
(함수 자체는 로그인을 확인하므로 데이터가 새지는 않지만, 열어 둘 이유가 없습니다.)

---

## 무엇이 어떻게 지켜지는가

| 위협 | 막는 방법 |
|---|---|
| 다른 회사 데이터 열람 | 데이터베이스가 행 단위로 차단. 앱에 버그가 있어도 안 나옵니다 |
| 앱을 뜯어 직접 조회 | 같습니다. 규칙은 서버에서 검사합니다 |
| 비밀번호 유출 | Supabase Auth 가 bcrypt 로 보관. 우리가 평문을 볼 수 없습니다 |
| API 키 탈취 | 키가 브라우저에 없습니다. 서버 함수만 갖고 있습니다 |
| 계정 도용 후 대량 조회 | 사람마다 하루 명함 인식 한도, 감사 기록 |
| 퇴사자 접근 | 계정을 중지하면 열려 있던 로그인까지 즉시 끊깁니다 |
| 남의 폰에 남은 자료 | 로그아웃하면 그 기기의 고객 정보를 지웁니다 |

## 아직 남은 일

- **명함 사진 업로드** — 보관함과 규칙은 만들어 뒀지만 앱이 아직 올리지 않습니다. 사진은 기기에만 있습니다
- **2단계 인증** — Supabase 가 지원합니다. 나중에 켤 수 있습니다
- **백업** — Free 요금제에는 자동 백업이 없습니다. 유료 전환 시 매일 백업이 붙습니다
- **개인정보 처리방침** — 고객 이름·전화·이메일을 다루므로 필요합니다
- **감사 기록 보존 기간** — 얼마나 오래 둘지 정해야 합니다
