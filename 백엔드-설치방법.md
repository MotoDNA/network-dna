# 백엔드 설치 방법 (Supabase)

구글 시트 대신 제대로 된 데이터베이스로 옮기는 절차입니다.
계정 생성은 제가 대신 할 수 없어서, 아래 1~2번은 직접 해주셔야 합니다.

---

## 1. Supabase 프로젝트 만들기

1. [supabase.com](https://supabase.com) 에서 가입하고 **New project** 를 누릅니다.
2. 이렇게 정합니다.
   - **Name**: `network-dna`
   - **Database Password**: 길게 만들어서 **비밀번호 관리자에 저장**하세요. 다시 볼 수 없습니다.
   - **Region**: **Northeast Asia (Seoul)** ← 꼭 서울로 하세요.
     고객 개인정보를 국내에 두는 편이 개인정보보호법상 훨씬 간단합니다.
   - **Plan**: Free 로 시작해도 됩니다.
3. 만들어지면 **Project Settings → API Keys** 에서 두 가지를 복사해 두세요.
   - **Project URL** (`https://xxxx.supabase.co`)
   - **Publishable key** (`sb_publishable_...`) — 예전 이름은 anon 키입니다

> **Publishable 키는 공개돼도 괜찮습니다.** 앱에 그대로 넣는 값입니다.
> 접근 제어는 키가 아니라 데이터베이스의 행 단위 규칙이 합니다.
> 반면 같은 화면의 **Secret 키(`sb_secret_...`)와 service_role 키는
> 절대 앱이나 깃허브에 넣지 마세요.** 그 키들은 모든 규칙을 무시합니다.

### 이 프로젝트의 값

```
Project URL : https://izrtclsqhsgkuwsffifn.supabase.co
Publishable : sb_publishable_G5wx-YdgwUU630x1vKmZmQ_15BgxvEX
Region      : ap-northeast-2 (서울)
```

`supabase/project.json` 에도 적어 두었습니다.

## 2. 표와 접근 규칙 만들기

1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. `supabase/migrations/0001_init.sql` 내용을 통째로 붙여넣고 **Run**
3. 오류 없이 끝나면 표와 규칙이 만들어진 것입니다.

### 바로 검증하세요

1. SQL Editor 에 새 쿼리를 열고 `supabase/tests/rls-test.sql` 을 붙여넣고 **Run**
2. 결과창 아래 **Notices** 에 `모든 시험 통과 (12/12)` 가 나와야 합니다.

이 시험은 회사가 둘, 사람이 넷인 상황을 만들어서
"다른 회사 것이 보이는가", "남의 고객을 고칠 수 있는가",
"일반 사용자가 스스로 관리자가 될 수 있는가" 를 12가지로 확인합니다.
하나라도 뚫리면 그 자리에서 멈추고 어디가 뚫렸는지 알려 줍니다.
시험 데이터는 끝나면 자동으로 되돌아가 남지 않습니다.

## 3. 서버 함수 올리기

터미널에서 합니다. (Supabase CLI 설치: `brew install supabase/tap/supabase`)

```bash
supabase login
supabase link --project-ref <프로젝트ID>
supabase functions deploy admin-user
supabase functions deploy read-card
```

### 함수가 쓸 비밀값 넣기

```bash
supabase secrets set SETUP_SECRET="$(openssl rand -base64 24)"
supabase secrets set ANTHROPIC_API_KEY="sk-ant-..."
supabase secrets set ALLOWED_ORIGIN="https://motodna.github.io"
```

- `SETUP_SECRET` — 회사를 처음 개설할 때만 쓰는 암호입니다. 설정한 값을 적어 두세요.
- `ANTHROPIC_API_KEY` — **이제 이 키는 서버에만 있습니다.** 각자 폰에 넣던 방식은 없어집니다.
- `ALLOWED_ORIGIN` — 앱이 올라간 주소. 다른 사이트에서 우리 함수를 부르지 못하게 막습니다.

## 4. 첫 회사와 관리자 계정 만들기

```bash
curl -X POST "https://<프로젝트ID>.supabase.co/functions/v1/admin-user" \
  -H "Content-Type: application/json" \
  -H "x-setup-secret: <위에서 만든 SETUP_SECRET>" \
  -d '{
    "action": "bootstrap",
    "code": "ACTIVA",
    "companyName": "액티바코리아",
    "loginId": "admin",
    "name": "관리자",
    "password": "여기에-10자-이상-비밀번호"
  }'
```

`{"ok":true,...}` 가 나오면 끝입니다.
이제 앱에서 **회사 코드 ACTIVA / 아이디 admin / 그 비밀번호**로 들어갑니다.

직원 계정은 관리자로 로그인한 뒤 앱의 설정 화면에서 추가합니다.

---

## 무엇이 어떻게 지켜지는가

| 위협 | 막는 방법 |
|---|---|
| 다른 회사 데이터 열람 | 데이터베이스가 행 단위로 차단합니다. 앱에 버그가 있어도 안 나옵니다 |
| 앱 코드를 뜯어 직접 조회 | 같습니다. 규칙은 서버에서 검사합니다 |
| 비밀번호 유출 | Supabase Auth 가 bcrypt 로 보관합니다. 우리가 평문을 볼 수 없습니다 |
| API 키 탈취 | 키가 브라우저에 없습니다. 서버 함수만 갖고 있습니다 |
| 계정 도용 후 대량 조회 | 사람마다 하루 명함 인식 한도가 있고, 감사 기록이 남습니다 |
| 퇴사자 접근 | 계정을 중지하면 열려 있던 로그인까지 즉시 끊깁니다 |

## 아직 남은 일

- **2단계 인증** — Supabase 가 지원하므로 나중에 켤 수 있습니다
- **백업** — Free 요금제는 자동 백업이 없습니다. 유료 전환 시 매일 백업이 붙습니다
- **개인정보 처리방침** — 고객 이름·전화·이메일을 다루므로 필요합니다
- **접속 기록 보존** — 감사 기록을 얼마나 오래 둘지 정해야 합니다
