{
  "name": "recall-app",
  "version": "1.0.0",
  "private": true,
  "description": "Re:Call 고객관리 — 안드로이드/아이폰 앱 껍데기",
  "scripts": {
    "sync": "node sync-web.js && cap sync",
    "android": "npm run sync && cap open android"
  },
  "dependencies": {
    "@capacitor-community/contacts": "^8.0.0",
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
