# 구글 시트 연동 방법

앱에 입력한 고객카드와 타임라인 기록이 구글 시트에 자동으로 쌓이게 만드는 설정입니다.
한 번만 해두면 됩니다. 10분이면 끝납니다.

---

## 1. 구글 시트 만들기

1. [sheets.new](https://sheets.new) 로 새 스프레드시트를 만듭니다.
2. 이름을 알아보기 쉽게 바꿉니다. 예) `고객관리 데이터`
3. 시트 탭은 그대로 두세요. `Customers` / `Timeline` 두 개는 스크립트가 알아서 만듭니다.

## 2. 스크립트 붙여넣기

1. 시트 상단 메뉴에서 **확장 프로그램 → Apps Script** 를 엽니다.
2. 편집기에 있는 기존 코드(`function myFunction() {}`)를 **전부 지웁니다.**
3. 같은 폴더의 **`appsscript-고객관리.gs`** 파일을 열어 내용을 전부 복사해 붙여넣습니다.
4. 💾 저장을 누릅니다.

## 3. 웹앱으로 배포하기

1. 오른쪽 위 **배포 → 새 배포** 를 누릅니다.
2. 톱니바퀴(유형 선택) → **웹 앱** 을 고릅니다.
3. 아래처럼 설정합니다.
   - **설명**: 아무거나 (예: 고객관리 v1)
   - **다음 사용자로 실행**: **나**
   - **액세스 권한이 있는 사용자**: **모든 사용자** ← 이게 제일 중요합니다
4. **배포** 를 누릅니다.
5. 권한 승인 창이 뜹니다. **액세스 승인 → 본인 계정 선택 → 고급 → (안전하지 않음) 이동 → 허용** 순서로 진행합니다.
   ⚠️ "확인되지 않은 앱" 경고는 내가 직접 만든 스크립트라 뜨는 정상 메시지입니다.
6. 마지막에 나오는 **웹 앱 URL** 을 복사합니다.
   `https://script.google.com/macros/s/...../exec` 형태여야 합니다.

## 4. 앱에 주소 넣기

1. 앱에서 **설정** 탭을 엽니다.
2. **구글 시트 연동 → Apps Script 웹앱 주소** 칸에 복사한 주소를 붙여넣습니다.
3. **연결 확인** 을 누릅니다. "연결 성공" 이 뜨면 끝입니다.

이제부터 고객카드를 만들거나 기록을 추가하면 몇 초 안에 시트에 반영됩니다.
화면 오른쪽 위에 `동기화됨` 표시가 뜹니다.

---

## 여러 명이 함께 쓰기

같은 웹앱 주소를 각자 기기의 설정에 넣으면 됩니다.
서로의 고객카드와 기록이 자동으로 합쳐집니다. 담당자 이름은 각자 다르게 설정해두세요.

## 시트 구조

| 시트 | 내용 |
|---|---|
| **Customers** | 고객 한 명이 한 행. 성함·회사·연락처·그룹·최근활동 |
| **Timeline** | 기록 한 건이 한 행. 어느 고객(`고객번호`)의 어떤 유형(미팅/요청사항/불만사항/특이사항/제공 샘플/할 일)인지 |

- 1행은 사람이 읽는 한글 머리글, 2행은 프로그램이 쓰는 영문 키입니다. **2행은 지우거나 고치지 마세요.**
- 데이터는 3행부터 쌓입니다.
- `삭제` 열이 `1` 인 행은 삭제된 기록입니다. 되살리려면 `0` 으로 바꾸면 됩니다.

---

## 시트에서 직접 입력하기

폰에서 시트로만 가는 게 아니라, **시트에서 입력한 것도 폰으로 옵니다.**
앱이 20초마다 시트를 확인하므로 길어야 20초 안에 나타납니다. (앱이 꺼져 있었다면 다음에 열 때)

기존 고객 명부를 옮겨 오거나, 사무실에서 여러 건을 한 번에 정리할 때 쓰면 편합니다.

### 고객 추가·수정 (Customers 시트)

3행 아래 빈 줄에 성함·회사명·전화번호를 적으면 끝입니다.

- **고유번호와 수정시각은 비워 두세요.** 입력하는 순간 스크립트가 알아서 채웁니다.
- **그룹** 은 쉼표로 적어도 됩니다. `주요 고객, 진행 중` → 자동으로 정리됩니다.
  앱에 없던 그룹 이름을 적으면 앱의 그룹 목록에도 새로 생깁니다.
- **등록일** 을 비워 두면 오늘로 채워집니다.
- 이미 있는 고객의 전화번호나 주소를 고치면, 고친 내용이 폰으로 넘어갑니다.

### 기록 추가 (Timeline 시트)

- **고객번호** 칸에는 고객의 **이름이나 전화번호를 적어도 됩니다.** 스크립트가 찾아서 번호로 바꿉니다.
  못 찾으면 그 칸이 **빨갛게** 표시되니, 이름을 다시 확인하거나 Customers 시트에서 고유번호를 복사해 넣으세요.
  동명이인이 있으면 자동으로 고르지 않습니다 — 전화번호나 고유번호를 쓰세요.
- **유형** 은 한글로 적으면 됩니다: `미팅` `요청사항` `불만사항` `특이사항` `제공 샘플` `할 일`
  비워 두면 `미팅` 으로 들어갑니다.
- **일시** 를 비워 두면 지금 시각으로 채워집니다.
- **완료** 는 `할 일` 에만 씁니다. `1` 이나 `완료` 라고 적으면 체크된 상태가 됩니다.
- 오른쪽 끝 **고객명(자동)** 칸은 사람이 보기 편하라고 스크립트가 채워 넣는 참고용입니다. 손대지 않아도 됩니다.

### 지울 때

**행을 통째로 지우면 폰에서는 안 지워집니다.** 앱은 없어진 행을 알아챌 방법이 없습니다.
`삭제` 열에 **1** 을 적어 주세요. 그러면 폰에서도 사라집니다.

### 주의할 점

- 폰과 시트에서 같은 고객을 동시에 고치면 **나중에 고친 쪽이 남습니다.**
- 스크립트를 새 버전으로 바꾼 뒤에는 **배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포** 를 해야
  동기화 부분이 갱신됩니다. (시트 입력 감지는 저장만 해도 바로 작동합니다.)

## 자주 묻는 것

**Q. 연결 확인에서 실패가 뜹니다.**
배포할 때 *액세스 권한이 있는 사용자* 를 **모든 사용자** 로 하지 않은 경우가 대부분입니다.
Apps Script에서 **배포 → 배포 관리 → 연필(수정) → 액세스 권한 변경 → 배포** 로 고칠 수 있습니다.

**Q. 스크립트를 고쳤는데 반영이 안 됩니다.**
저장만으로는 부족합니다. **배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포** 를 해야 합니다.
이때 URL은 바뀌지 않습니다.

**Q. 명함 사진도 시트에 올라가나요?**
아니요. 사진은 용량이 커서 기기에만 저장됩니다. 시트에는 글자 정보만 올라갑니다.
기기를 바꾸면 명함 사진은 다시 찍어야 합니다.

**Q. 명함을 찍으면 자동으로 잘리나요?**
네. 촬영하면 카드 테두리를 찾아 배경을 없애고, 비스듬히 찍힌 것도 반듯하게 펴고,
조명 얼룩까지 정리해서 저장합니다. 사진 아래 **자동 보정 / 원본** 으로 언제든 되돌릴 수 있고,
눕혀 찍었을 때는 옆의 **↻** 로 세울 수 있습니다.
흰 명함을 흰 책상에 그림자 없이 놓으면 테두리를 못 찾을 수 있는데,
이때는 "테두리를 못 찾았습니다"라고 알려주고 원본을 그대로 씁니다.
색이 다른 바닥에 놓거나 살짝 그늘이 지게 찍으면 잘 잡힙니다.

**Q. 전화번호를 폰 주소록에 저장할 수 있나요?**
고객카드를 만들면 "연락처에도 저장할까요?" 창이 한 번 뜹니다. **연락처 저장** 을 누르면 됩니다.
그때 넘어갔더라도 고객 상세 화면의 **연락처** 버튼으로 언제든 저장할 수 있습니다.
매번 묻는 게 번거로우면 창 아래 **다시 묻지 않기** 를 누르거나,
설정 → 기타 → **연락처 저장 물어보기** 를 끄면 됩니다. 버튼은 그대로 남습니다.
웹에서는 주소록에 곧바로 쓸 수 없어서, 연락처 파일(.vcf)을 만들어 폰에 넘깁니다.
아이폰·안드로이드 모두 "연락처에 추가" 화면이 떠서 한 번 확인만 하면 저장됩니다.
이름·회사·직함·전화·이메일·주소와 함께 최근 기록 5건이 메모로,
명함 사진이 프로필 사진으로 같이 들어갑니다.

**Q. 오프라인에서도 쓸 수 있나요?**
네. 기록은 기기에 먼저 저장되고, 인터넷이 연결되면 자동으로 시트에 올라갑니다.
그동안 오른쪽 위에 `대기` 표시가 떠 있습니다.
