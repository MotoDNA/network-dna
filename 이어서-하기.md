# 이어서 하기 — Re:Call 인수인계

세션을 새로 시작할 때 이 파일부터 읽으면 됩니다.
자세한 배경은 [진행상황.md](진행상황.md) 에 있고, 이 문서는 **지금 당장 필요한 것만** 추렸습니다.

마지막 갱신: 2026-08-30

---

## 지금 어디까지 왔나 (2026-08-30)

Re:Call 을 유료 서비스로 여는 작업 중입니다. **살아 있는 것 / 막힌 것**은 이렇습니다.

| | 상태 |
|---|---|
| `https://dnalabs.kr` 회사 홈페이지 | **운영 중** (Vercel) |
| `recall.dnalabs.kr` · `rebind.dnalabs.kr` | **운영 중** (GitHub Pages) — 건드리지 말 것 |
| 이용약관 · 환불정책 · 개인정보 처리방침 | 배포됨 (앞 둘은 **변호사 검토 전 초안**) |
| 회사 메일 `hello@dnalabs.kr` | **수신·발신 정상.** DKIM 은 일단 넘어갔습니다 (아래 1번) |
| 회원가입 | **닫아 뒀습니다** (`web/signup.html` 의 `SIGNUP_OPEN = false`) |
| 결제(PG) | **계약 전.** `web/pg.js` 가 스텁 |
| 통신판매업 신고 | **신청 완료. 심사 대기 중** (2026-08-30) |
| SQL `recall_0002` · `0003` · `0004` | **셋 다 적용 완료** (2026-08-30 14:07 확인) |
| 명함 인식 한도 | **살아 있습니다** (2026-08-30 배포·점검 완료) |

**막혔던 네 가지 — 2026-08-30 정리**

1. ~~**DKIM**~~ — **넘어가기로 했습니다.** 개인 메일함에 정상 도착을 확인했습니다.
   다만 받는 쪽마다 판정이 다릅니다(Gmail 통과 ≠ 네이버·다음 통과). 고객사에 안내 메일을
   돌리기 시작한 뒤 반송·스팸이 늘면 아래 「회사 메일」 절의 순서대로 켜세요.
2. ~~**통신판매업 신고**~~ — **신청했습니다.** 심사 결과를 기다리면 됩니다.
   ⚠ 신고번호가 나오면 **꼬리말에 넣어야 합니다.** 자리표시자를 빼 뒀기 때문에 아무 알림도 안 뜹니다.
3. **PG 계약** — **나중으로 미뤘습니다.** 정해지면 `pg.js` 와 서버 두 함수만 채우면 됩니다
4. ~~**요금제 기능 차등**~~ — **정했습니다.** 아래 「명함 인식 한도」 절

---

## 지금 바로 해야 할 일 (우선순위 순)

### 1. SQL 적용 — 끝났습니다  ✓ (2026-08-30)

`recall_0002` · `recall_0003` · `recall_0004` 셋 다 들어갔습니다. `./check-db.sh` 로 확인했습니다.
Re:Bind 의 `rebind_0003_works_realtime` 도 함께 넣었습니다.

아래는 **다음에 마이그레이션을 넣을 때 쓰는 방법**입니다.

⚠ **`pbcopy` 는 사람이 직접 터미널에서 실행할 때만 진짜 클립보드에 담깁니다.**
에이전트가 대신 돌리면 격리된 셸의 클립보드로 들어가 화면에서 붙여넣기가 안 됩니다.
그럴 때는 스크립트가 알려 주는 「전문 보기」 경로의 파일을 열어서 복사하세요.

#### 넣는 방법

먼저 지금 상태를 보세요 (로그인 불필요):

```bash
./check-db.sh
```

2026-08-28 확인 시점에 **0002·0003 모두 미적용**입니다.
0002 는 두 번 "실행했다"고 기록됐지만 서버에는 들어가 있지 않았습니다.

붙여넣기 사고가 나지 않게 스크립트를 씁니다. **순서가 있습니다 — 0002 먼저:**

```bash
./apply-migration.sh recall_0002_visibility.sql
./apply-migration.sh recall_0003_billing.sql
./apply-migration.sh recall_0004_card_limits.sql
```

실행하면 안전장치 + 본문 + 확인 질의를 하나로 묶어 클립보드에 담습니다.
[SQL 편집기](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new)에서
**아무것도 선택하지 말고** 붙여넣고 Run 하세요.

막아 주는 것 —

- **다른 프로젝트에서 실행** — 맨 앞 `do $guard$` 가 표·함수를 확인하고 아니면 즉시 멈춥니다.
- **일부만 선택 실행** — 통째로 클립보드에 담기고, 전체가 `begin; … commit;` 으로 묶여 있습니다.
  0002 본문 중간에 `drop policy customers_read` 가 있어서, 거기까지만 돌면
  **고객 목록이 아무에게도 안 보이게 됩니다.** 트랜잭션이 그것을 막습니다.

**0002 를 안 넣어도 앱은 정상 동작합니다.** (코드로 확인)
`probeVisibility()` 가 컬럼 유무를 보고 없으면 관련 UI 를 숨기고 값도 안 보냅니다.
**0003 은 다릅니다** — 이게 없으면 회원가입이 아예 안 됩니다.

### 2. 개인정보 보호책임자 — 채웠습니다  ✓

2026-08-28 완료. `DNA Labs 최환준` · `hello@dnalabs.kr` · `010-6451-5807`.
[network-dna.html:3340](network-dna.html) 의 `privacyHTML()` 안 "8. 문의처" 에 있습니다.
`PRIVACY_REV` 이 이미 `2026-08-28` 이라 시행일은 그대로 뒀습니다.

**앞으로 방침을 고칠 때는 `privacyHTML()` 한 군데만 고치면 됩니다.**
PDF 는 그 함수에서 뽑아 만듭니다:

```bash
./make-privacy-pdf.sh              # 일반 배포용 ("고객사")
./make-privacy-pdf.sh 액티바코리아   # 회사 이름을 넣어서
```

`network-dna.html` 의 `<style>` 블록과 `privacyHTML()` 을 원본에서 읽어
그대로 실행해 씁니다. 문구도 CSS 도 사본이 없으니 **앱 화면과 PDF 가 어긋날 수 없습니다.**
문구를 고치면 이 스크립트만 다시 돌리고, `PRIVACY_REV` 시행일도 함께 올리세요.

자리표시자(`[...]`)가 남아 있으면 파일 이름에 `_초안` 이 붙어 나옵니다.
미완성 방침을 고객사에 보내는 일을 막으려는 장치입니다.

스크립트를 고칠 일이 있으면 알아둘 것 — 덧쓰는 CSS 클래스 이름이 앱 것과 겹치면
안 됩니다. 처음에 `.sheet` 를 썼다가 PDF 가 통째로 빈 종이로 나왔습니다
(`.sheet` 는 화면 밖에 세워 두는 바텀시트입니다). 지금은 겹치면 즉시 멈추는
가드가 들어 있습니다.

### 3. 명함 인식 한도 — 정했습니다 (2026-08-30)

숫자는 **`catalog.json` 한 군데**에만 있습니다. 코드에도 DB 에도 적지 마세요.

| 요금제 | 1인 / 하루 | 회사 / 한 달 | 월 한도 소진 시 원가 | 월 요금 대비 |
|---|---|---|---|---|
| Personal | 100 | 200 | ₩2,000 | 11% |
| Business 5 | 75 | 600 | ₩6,000 | 12% |
| Business 20 | 75 | 1,250 | ₩12,500 | 13% |
| Business 49 | 100 | 3,000 | ₩30,000 | 12% |
| Enterprise | 150 | 제한 없음 | — | — |

원가는 명함 1장에 **7~10원**입니다 (`claude-sonnet-5`, 이미지 1,100~2,600 토큰 +
출력 ~120 토큰, 환율 1,400원 가정). 위 표는 10원으로 계산했습니다.

**이 숫자는 영업용이 아니라 사고 방지선입니다.** 요금표나 약관에 적지 마세요 —
적으면 지켜야 하고, 나중에 내리면 불리한 약관 변경이 됩니다. 사용 통계가 쌓인 뒤에 정하세요.

**「하루」·「한 달」은 한국시간 달력 기준입니다.** 자정과 매월 1일에 초기화됩니다.
예전에는 최근 24시간으로 셌는데, 그러면 오후에 한도를 채운 사람이 다음날 오후까지
막혀서 전시회 이틀차 아침에 사고가 납니다.

⚠ **경계가 두 군데에 있습니다.** `read-card` 의 `kstDayStart`·`kstMonthStart` 와
`0004` 의 `card_usage()` 가 같은 경계를 써야 합니다. 한쪽만 고치면 화면에 보이는
숫자와 실제로 막히는 시점이 어긋납니다.

⚠ **Personal 은 하루 100 / 한 달 200 이라, 이틀 바쁘면 그 달이 끝납니다.**
전시회에 가는 1인 사업자가 정확히 여기 걸립니다. 첫 고객 반응을 보고
월 한도를 400 쯤으로 올릴지 정하세요 (원가는 ₩4,000, 요금의 21%).

**아직 안 한 것 —**

- ~~`recall_0003` 과 `recall_0004` 적용~~ ✓ 2026-08-30
- ~~`read-card` 배포~~ ✓ 2026-08-30. `bash check-api-key.sh` 로 부팅까지 확인했습니다
- **앱에서 "오늘 12/100" 을 보여주는 화면이 없습니다.** `card_usage()` 는 만들어 뒀지만
  부르는 곳이 없습니다. 지금은 **한도에 걸린 뒤에야** 알게 됩니다. 이것이 남은 유일한 항목입니다.


### 4. 유료 서비스 전환 — 지은 것과 안 지은 것

2026-08-28 에 홈페이지·소개·가입 흐름을 지었습니다. `web/` 아래에 있습니다.

```bash
cd web && python3 -m http.server 8792   # http://localhost:8792/index.html
```

**요금과 업종 판정은 `catalog.json` 한 군데에만 있습니다.**
홈페이지·가입 화면·서버가 모두 이 파일을 봅니다. 값을 고치면:

```bash
./sync-catalog.sh      # web/ 과 서버용 catalog.ts 를 다시 만듭니다
```

숫자를 두 군데 적어 두면 "화면엔 49,000원인데 실제론 99,000원이 빠지는" 일이 납니다.
그래서 데이터베이스에도 요금을 적지 않았습니다 — DB 는 `seat_limit`(자리 수)만 압니다.

| 지은 것 | 어디 |
|---|---|
| 회사 홈페이지 | `web/index.html` |
| Re:Call 소개·요금·업종 | `web/recall.html` |
| 회원가입 (업종 게이팅 → 요금제 → 정보 → 약관 → 결제 → 발급) | `web/signup.html` |
| 요금·업종 단일 출처 | `catalog.json` |
| 구독·좌석제한·회사코드 | `supabase/migrations/recall_0003_billing.sql` |
| 가입 처리 서버 | `supabase/functions/signup/index.ts` |
| 이용약관 · 환불정책 · 처리방침 | `web/terms.html` · `web/refund.html` · `web/privacy.html` |
| 404 · 파비콘 · 공유이미지 · robots · sitemap | `web/404.html` · `favicon.svg` · `og-*.png` · `robots.txt` · `sitemap.xml` |
| 앱 요금제 화면 (상태·남은 자리·해지) | `network-dna.html` 설정 탭 |
| 해지 · 요금제 변경 서버 | `supabase/functions/subscription/index.ts` |
| 앱 요금제 변경 화면 (상위 즉시·하위 예약) | `network-dna.html` 설정 탭 |

**웹페이지는 여기까지 끝났습니다 (2026-08-28)**

- 6쪽 모두 **모바일에서 가로 스크롤 없음** 확인 (375px)
- 좁은 화면 여백 조정 — 히어로 96→52px, 구획 84→56px, 버튼 세로 배치
- 파비콘 · 공유 이미지(1200×630) · 404 · robots.txt · sitemap.xml
- 내부 링크 전부 실제 파일로 연결됨 (`#` 남은 것 없음)
- 회사 홈페이지에 **문의 구획** 추가 (Re:Call 말고 "일을 맡기는" 경로가 없었습니다)

공유 이미지를 고치려면 `./build-og.sh` 를 돌리세요.

**아직 채우지 않은 값은 화면에 노랗게 표시됩니다.** `span[data-todo]` 규칙이
사업자등록번호·통신판매업 신고번호·주소를 눈에 띄게 칠합니다.
법정 표시의무라서, 대괄호인 채로 배포되면 안 되기 때문입니다. 값만 주시면 지웁니다.

**아직 안 된 것 —**

| 할 일 | 왜 막혀 있나 |
|---|---|
| **결제대행사(PG) 계약** | 계약 전이라 `web/pg.js` 가 스텁입니다. 가입 화면에 "테스트 모드" 띠가 뜨고, 실제 돈은 움직이지 않습니다. 계약 후 `pg.js` · `signup` 의 `pgAuthorizeZero` · `subscription` 의 `pgChargeOnce` 셋을 채우면 됩니다. **zerofee.kr 은 이 자리에 못 씁니다** — 아래 참고 |
| **통신판매업 신고** | 구매안전서비스 확인증을 네이버 스마트스토어로 받는 중(심사 대기). ⚠ **신고 후 꼬리말에 신고번호를 넣어야 합니다** — 자리표시자를 뺐기 때문에 알림이 안 뜹니다. 절차는 [통신판매업-신고.md](통신판매업-신고.md) |
| **회원가입 잠금 해제** | 지금 `SIGNUP_OPEN = false` 로 닫아 뒀습니다. PG 계약 · `signup` 함수 배포 · 신고번호 셋이 되면 `true` 로 |
| **가입 안내 메일** | 안 만들었습니다. 그래서 화면에서 "메일로 보내 드립니다"라는 문구를 뺐습니다. 지금은 비밀번호를 화면에서 한 번만 보여 줍니다 |
| **dnalabs.kr DNS** | Vercel 배포·도메인 등록까지 끝났습니다(2026-08-28). 가비아에서 **A 레코드 두 줄**만 추가하면 열립니다 → [dnalabs-배포.md](dnalabs-배포.md). ⚠ 네임서버를 Vercel 로 바꾸면 `recall.dnalabs.kr` 이 죽습니다 |
| **ALLOWED_ORIGIN 에 새 주소 추가** | `signup` 함수를 배포하기 전에 `dnalabs.kr` 을 넣어야 브라우저가 막지 않습니다. ⚠ Re:Bind 와 함께 쓰는 값이라 **기존 주소를 지우면 그쪽이 멈춥니다** — 늘 전부 함께 적으세요 |
| **고객사 실명 공개 동의** | 홈페이지 사례에 액티바코리아를 실명으로 넣지 않았습니다. 동의를 받으면 바꾸세요 |

#### 회사 메일 — hello@dnalabs.kr (2026-08-30 개통)

가비아 **하이웍스** 메일. 도메인이 가비아에 있어 같은 곳에서 관리합니다.

| | |
|---|---|
| 주소 | `hello@dnalabs.kr` |
| 로그인 | 하이웍스 · ID `hello` |
| 라이선스 | **메일 1/1** — 직원이 생기면 추가 구매 필요 |
| 웹메일 | mails.office.hiworks.com |
| 관리 | admins.office.hiworks.com (사용자·라이선스), mail-admin.office.hiworks.com (메일 설정) |

DNS 는 가비아에 직접 넣었습니다 (자동 설정 버튼이 안 먹었습니다).

```
MX   @  mx1.hiworks.com.  우선순위 10
MX   @  mx2.hiworks.com.  우선순위 20
TXT  @  v=spf1 include:_spf.hiworks.co.kr ~all
```

**겪은 것 두 가지 —**

1. **공용메일(`hello@`)을 만들려다 계속 실패**했습니다. 결국 **사용자 ID 를 `dnalabs` → `hello` 로 바꾸는** 것으로 해결했습니다. 별칭도 공용메일도 필요 없고 제일 깔끔합니다. 나중에 직원이 생겨 문의를 나눠 받을 일이 생기면 그때 공용메일을 다시 시도하세요.

2. **`551 we don't relay`** 로 메일이 반송됐습니다. DNS 는 멀쩡한데 하이웍스가 도메인을 아직 인식하지 못한 상태였습니다. 가비아의 「도메인 연결 및 이메일 설정 확인」 창에서 **새로고침**을 눌러 재확인시키니 풀렸습니다. 같은 증상이 나오면 거기부터 보세요.

> ⚠ **SPF 는 도메인당 한 줄만 유효합니다.** 나중에 서비스 발신 메일(Resend 등)을 붙일 때
> 새 TXT 줄을 만들면 둘 다 깨집니다. 위 줄에 `include:` 를 **덧붙여야** 합니다.

**DKIM — 아직 안 켰습니다. 보낸 메일이 스팸함으로 갑니다 (2026-08-30 확인)**

켜는 곳: [하이웍스 매뉴얼](https://www.hiworks.com/manual/hiworks/113/1242) ·
[가비아 · DKIM 안내](https://library.gabia.com/contents/groupware/8226/)

순서 —
1. 하이웍스 메일 관리에서 **DKIM 생성** → 공개키(TXT 레코드 값)를 받습니다
2. 그 값을 **가비아 DNS 관리툴에 TXT 로 추가** (호스트 이름은 하이웍스가 알려주는 대로, 보통 `셀렉터._domainkey`)
3. **적용까지 최대 48시간** 걸립니다
4. DMARC 는 SPF·DKIM 이 자리 잡은 **48시간 뒤에** 추가하세요. 먼저 넣으면 정상 메일도 차단될 수 있습니다

⚠ DNS 에 넣을 때 **`recall`·`rebind` CNAME 은 건드리지 말고 추가만** 하세요.

**다만 DKIM 만이 원인이 아닐 수 있습니다.** 시험 메일의 제목과 본문이 둘 다 `test` 였는데,
그건 그 자체로 전형적인 스팸 신호입니다. 실제 문장이 담긴 메일로 다시 시험해 보세요.
새 도메인은 평판이 쌓이기 전까지 한동안 스팸함으로 가는 것이 정상이고,
받는 쪽에서 "스팸 아님"을 몇 번 눌러 주면 잡힙니다.

확인 명령:

```bash
dig +short <셀렉터>._domainkey.dnalabs.kr TXT
dig +short dnalabs.kr TXT          # SPF 는 한 줄만 있어야 합니다
```

#### 약관에 적었는데 아직 코드가 없는 것  ⚠

약관은 지키지 못하면 그대로 분쟁이 됩니다. 서비스를 열기 전에 이 둘을 채우세요.

| 약속한 곳 | 내용 | 필요한 것 |
|---|---|---|
| 환불정책 제2조 4항 | 무료 기간 끝나기 **3일 전 이메일 고지** | 메일 발송 + 매일 도는 예약 작업 |
| 이용약관 제11조 4항 | 해지 30일 후 **데이터 자동 삭제** | 예약 작업 |

반대로 **이미 지켜지는 것** — 이용약관 제11조 3항(해지 전 데이터 내려받기)은
앱 설정의 `고객 CSV` · `타임라인 CSV` 로 됩니다.

**하위 요금제 예약을 제때 적용해 줄 것이 없습니다.**
`apply_due_plan_changes()` 는 만들어 뒀지만, 지금은 누군가 앱에서 구독 화면을
열 때만 돌아갑니다. 아무도 안 열면 예약이 적용되지 않은 채 옛 금액이 남습니다.
**매월 요금을 걷는 작업을 만들 때, 걷기 전에 반드시 이 함수를 먼저 부르세요.**
순서가 바뀌면 내려 달라고 한 고객에게 옛 금액을 청구하게 됩니다.

그리고 매월 실제로 돈을 걷는 **정기 청구 작업**이 아직 없습니다.
지금은 구독 행만 만들고 아무것도 청구하지 않습니다. PG 계약 후에 만들어야 합니다.

#### "결제" 라는 이름의 것들이 층이 다릅니다 — 헷갈리지 않게

찾아보다 보면 이름은 비슷한데 층위가 전혀 다른 것들이 나옵니다.
**우리에게 필요한 것은 딱 한 층, PG(결제대행사) 계약입니다.**

| 층 | 무엇 | 예 | 우리에게 |
|---|---|---|---|
| 카드사·금융기관 | 실제로 돈이 오가는 곳 | 신한·삼성카드 | 직접 상대 안 함 |
| **PG (결제대행사)** | **가맹점 대신 카드 승인을 받아 줌** | **토스페이먼츠 · KG이니시스 · 나이스페이 · NHN KCP** | **← 여기 계약이 필요합니다** |
| PG 통합 API | 여러 PG 를 한 API 로 감쌈 | 포트원 | 편의 계층. 권장 |
| 간편결제 수단 | PG 위에 얹는 결제수단 | 네이버페이 · 카카오페이 | 나중에 추가 (아래) |
| 판매 플랫폼 | 남의 가게에서 상품을 팖 | zerofee.kr | 우리 사이트에서 못 씀 |
| PG 시스템 솔루션 | **PG 를 직접 차리려는 회사용 소프트웨어** | 티엔씨테크날로지 PG&PrePaid (네이버클라우드 마켓플레이스) | 완전히 다른 사업 |

마지막 줄이 특히 헷갈리기 쉽습니다. 네이버 클라우드 마켓플레이스의
**PG&PrePaid** 는 네이버가 주는 결제 서비스가 아니라, 티엔씨테크날로지가 파는
**결제·정산 시스템 구축 소프트웨어**입니다. [그 회사 소개](https://www.tnctec.co.kr/)에
"전자금융업 등록 어디서부터 시작해야 될지 고민되시죠?" 라고 적혀 있는 데서 보이듯,
**전자금융업자로 등록해 스스로 결제·선불충전을 굴리려는 회사**가 고객입니다.

이걸 사도 카드 승인을 대신 받아 줄 PG 계약은 여전히 따로 필요하고,
'선불 충전' 모델은 월 구독과 맞지도 않습니다. 게다가 선불전자지급수단을 발행하면
금융 규제 대상이 되는데, 그건 우리 「도입 판정 기준」에서 **팔면 안 된다고 적어 둔 바로 그 영역**입니다.

#### 네이버페이는 "나중에 추가"입니다 (2026-08-28 확인)

네이버페이 **결제형**은 정기/반복결제를 지원합니다. 다만 지금은 신청 자격이 안 됩니다 —
[포트원 계약 안내](https://help.portone.io/content/naverpay-contract)에 두 가지가 못 박혀 있습니다.

1. **일반 PG 결제모듈이 이미 연동되어 있어야** 가입 신청이 됩니다
2. **최소 1개월 이상 매출**이 발생해야 심사가 진행됩니다

즉 네이버페이는 PG 를 **대신하는 것이 아니라 그 위에 얹는 결제수단**입니다.
매출이 0인 지금은 순서상 신청 자체가 안 됩니다.

**그래서 순서는 이렇습니다** — 일반 PG 계약 → 서비스 오픈 → 한 달 매출 →
그때 네이버페이 결제형 추가 신청 (1차 심사 최대 1주 + 검수 약 2주).

네이버페이를 나중에 붙일 생각이시면 **처음 PG 를 포트원으로 잡는 편이 낫습니다.**
네이버페이 정기결제 연동이 포트원 쪽에 문서화되어 있어서
(`customer_uid` 로 등록하고 `/subscribe/payments/again` 으로 청구),
나중에 결제수단만 추가하는 형태가 됩니다.

#### zerofee.kr 은 결제대행사가 아닙니다 (2026-08-28 확인)

[zerofee.kr](https://zerofee.kr/) 은 **전자책·멤버십·VOD 강의를 파는 크리에이터 판매 플랫폼**입니다
(수수료 0%). 그쪽 개발사인 [제로소싱의 소개](https://www.zerosourcing.kr/portfolio/zerofee-creator-digital-content-selling-platform)에도
"크리에이터 상품 판매 중개 플랫폼"이라고 적혀 있고, 기능 목록의 "PG 결제 연동"은
**그 플랫폼이 PG 를 갖다 쓴다**는 뜻이지 남에게 PG 를 내준다는 뜻이 아닙니다.

Re:Call 에 못 쓰는 이유 —

1. **빌링키를 안 줍니다.** 우리 서버가 매달 스스로 청구하려면 빌링키가 필요한데,
   판매 플랫폼은 그것을 내주지 않습니다.
2. **판매 주체가 그쪽이 됩니다.** 방금 쓴 이용약관·환불정책은 DNA Labs 가 청구하고
   환불하고 세금계산서를 끊는 것을 전제로 합니다. 그 전제가 깨집니다.
3. **가입 흐름이 끊깁니다.** 업종 판정 → 0원 승인 → 회사코드 자동발급이
   그쪽 결제창 안에서 일어날 수 없습니다.

되는 것이 있다면, 그쪽에 "Re:Call 멤버십"을 상품으로 올려 팔고 **회사코드를 손으로
발급**하는 방식입니다. 몇 건은 되지만 자동 갱신·해지·요금제 변경이 전부 수동이 됩니다.

필요한 것은 **빌링(정기결제) 계약이 되는 PG** 입니다 — 토스페이먼츠 · 포트원 · 나이스페이 · KG이니시스.
카드 수수료는 대략 2~3.4%, 최초 가입비가 20만원대인 곳이 많습니다.

**「Re:Call 도입 판정 기준」 문서(PDF·아티팩트)의 F 표가 낡았습니다.**
`개인정보 처리방침 — 없음`, `자동 백업 — 없음(무료 요금제)` 두 줄이 이제 사실이 아닙니다.
영업 자료라 고객사에 그대로 나가면 곤란합니다.

### 5. 남은 것

| 할 일 | 설명 |
|---|---|
| 검증용 데이터 정리 | 앱에 "검증고객" 이 아직 남아 있을 수 있음 |
| 2단계 인증 | Supabase 가 지원. 아직 안 켰음 |
| ~~자동 백업~~ | ✓ 해결. Supabase Pro 로 올렸으니 매일 백업됩니다 (2026-08-28) |
| 예전 주소 정리 | 안정되면 `ALLOWED_ORIGIN` 에서 `motodna.github.io` 를 빼면 됨 |

---

## 접속 정보

```
운영 주소      https://recall.dnalabs.kr
앱 설치 페이지  https://recall-install.vercel.app
저장소         https://github.com/MotoDNA/network-dna   (공개)

Supabase ref   izrtclsqhsgkuwsffifn
Project URL    https://izrtclsqhsgkuwsffifn.supabase.co
Publishable    sb_publishable_G5wx-YdgwUU630x1vKmZmQ_15BgxvEX   (공개해도 되는 값)
Region         ap-northeast-2 (서울)

앱 로그인      회사코드 ACTIVA / 아이디 admin / 비밀번호는 직접 정하신 것
설치 암호      .setup-secret 파일 (깃 제외)
```

Vercel 계정 `chhanj40-5991` · 프로젝트 `recall-install` (`npx vercel` 로 씀).

---

## 구조 한눈에

```
network-dna.html          웹과 앱의 본체. 파일 하나. 여기만 고치면 됩니다
      ├─ 깃허브 페이지 → https://recall.dnalabs.kr
      └─ app/npm run sync → 안드로이드 앱 (주소록에 바로 저장)

supabase/
  migrations/                     ⚠ 파일명 앞이 어느 프로젝트 것인지입니다
    0001_init.sql                 표 5개 · 접근 규칙 16개   (적용됨 · 두 프로젝트 공용)
    recall_0002_visibility.sql    공개 범위 · team_stats    (적용됨)
    recall_0003_billing.sql       구독 · 좌석제한 · 회사코드  (적용됨)
    recall_0004_card_limits.sql   명함 한도 인덱스 · card_usage()  (적용됨)
    rebind_0003_works_realtime.sql  Re:Bind 실시간 알림      (적용됨)
  functions/read-card/            명함 인식 대행 + 설치 점검(selftest)
  functions/admin-user/           계정 관리 (service_role 을 쓰는 유일한 곳)
  functions/_shared/cors.ts       허용 주소 (쉼표로 여러 개)

app/                      Capacitor 안드로이드 껍데기
setup-admin.sh            회사·관리자 계정 만들기
apply-migration.sh        마이그레이션을 붙여넣기 좋은 형태로 클립보드에 담습니다
check-db.sh               서버에 뭐가 들어가 있는지 확인 (로그인 불필요)
sync-catalog.sh           catalog.json 을 web/ 과 서버로 내려보냅니다
make-privacy-pdf.sh       처리방침 PDF 를 privacyHTML() 에서 뽑아 만듭니다

catalog.json              요금제·업종 판정의 단일 출처. 여기만 고칩니다
web/                      dnalabs.kr 홈페이지 · Re:Call 소개 · 회원가입
check-api-key.sh          API 키 점검 (로그인 불필요)
```

앱이 스스로 판단하는 것 세 가지 — **이 패턴을 깨지 마세요.**

| 변수 | 하는 일 |
|---|---|
| `IS_APP` | Capacitor 앱인지. 참이면 주소록에 바로 씀 |
| `IS_IOS` | 아이폰인지. 참이면 iframe 으로 연락처 화면을 띄움 |
| `hasVisibility` | 서버에 `visibility` 컬럼이 있는지. 없으면 관련 UI 를 숨김 |

---

## 홈페이지 — 세 서비스 기준으로 고쳤습니다 (2026-08-30)

안내 페이지가 아직 "Re:Call 하나" 기준이었습니다. 지금은 셋인데 글로만 적혀 있어서
무엇을 하는 것인지 열어 보기 전에는 알 수 없었습니다.

- `#services` 칸을 새로 만들고 **진짜 앱을 띄워 찍은 화면 여섯 장**을 넣었습니다
- 우측 상단·첫 화면 단추를 `Re:Service 접속하기`(`/reservice`)로
- "만든 것" 에서 Re:Call · Re:Bind 카드를 빼고 한 장으로 묶었습니다 — 같은 말을
  두 군데에 적으면 어느 쪽이 최신인지 알 수 없어집니다
- 마지막 권유 칸을 "주소는 하나입니다" 로 바꿨습니다
- 꼬리에 세 서비스 링크를 모두 넣었습니다
- 덤: 모든 칸의 머리글자(`.kicker`)가 왼쪽으로 밀려 있던 것을 고쳤습니다.
  `.shead p` 가 폭을 좁히는데 `.kicker` 가 왼쪽 여백을 0 으로 못 박아
  가운데 정렬이 안 먹고 있었습니다

### 화면은 `./build-shots.sh` 로 다시 찍습니다

손으로 그리지 않은 이유: 앱을 고칠 때마다 그림이 거짓말이 됩니다.
**앱을 고쳤으면 이걸 다시 돌리고 Vercel 에 올리세요.**

자료는 예시지만 **연락처·주소·사업자번호는 우리 것**입니다 —
남의 번호를 예시로 박아 두면 그 사람에게 전화가 갑니다.

찍고 나서 520px 로 줄이고 128색으로 낮춥니다(729KB → 189KB).
화면에서는 폰 그림 안에 212px 로 보이니 그걸로 충분합니다.

#### ⚠ 크롬 `--screenshot` 은 화면 폭을 지키지 않습니다

맥에서 창이 **500px 밑으로 안 내려갑니다.** `--window-size=390,844` 를 주면
화면은 500px 로 짜 놓고 **그림만 390px 로 잘라 냅니다** — 오른쪽이 잘립니다.
`--force-device-scale-factor` 를 함께 주면 창 크기 지시가 아예 무시됩니다.
뿌리에 `zoom` 을 걸어도 미디어쿼리는 넓은 폭으로 읽혀 PC 배치가 나옵니다.

그래서 `shoot.mjs` 가 크롬 안쪽(DevTools Protocol)의
`Emulation.setDeviceMetricsOverride` 를 직접 씁니다. 노드 26 에 `WebSocket` 이
들어 있어 따로 설치할 것은 없습니다.

> `build-shots.sh` · `shoot.mjs` · `shots/` · `web/` 은 **아직 git 에 안 올라가
> 있습니다.** 이 저장소의 `web/` 는 예전부터 untracked 인 채로 Vercel 에만
> 올라가고 있습니다.

---

## ⚠ 홈페이지(web/)가 이제 git 에 있습니다 — 2026-08-30

**그 전까지 `web/` 는 git 에 없었습니다.** Vercel 이 이 저장소의 `web` 폴더를
뿌리로 잡고 있는데 그 폴더가 GitHub 에 없어서, **push 할 때마다 빌드가 실패**하고
실패 메일이 갔습니다.

```
The specified Root Directory "web" does not exist
```

사이트 자체는 멀쩡했습니다 — `npx vercel --prod` 로 로컬 파일을 통째로 올리고
있었기 때문입니다. 대신 홈페이지에 **판 기록이 하나도 없었습니다.**
잘못 고쳐도 되돌릴 데가 없었다는 뜻입니다.

이제 `git push` 만 해도 배포됩니다. `npx vercel --prod` 도 그대로 됩니다.

올리기 전에 확인한 것:

| | |
|---|---|
| `signup.html` 의 `sb_publishable_…` | Supabase **공개용** 키. 이미 dnalabs.kr 에서 그대로 내려가고 세 앱에도 들어 있습니다 |
| `pg.js` | 아직 스텁이라 결제사 열쇠가 없습니다 |
| `.vercel` · `.env*` | `web/.gitignore` 가 막습니다 |

이 저장소는 **공개**이고 GitHub Pages 로도 열려서, 같은 쪽이 두 주소에서 보입니다.
쪽마다 `<link rel="canonical">` 을 넣어 원본이 dnalabs.kr 이라고 못 박았습니다.

## 줄 넘길 때 낱말이 잘리던 것 — 2026-08-30

브라우저 기본값(`word-break:normal`)은 한글을 **아무 글자에서나** 자릅니다.
"계약합니 / 다." 처럼 낱말이 두 줄로 쪼개졌습니다. `body` 에
`word-break:keep-all` 을 두어 띄어쓰기에서만 넘깁니다.

띄어쓰기 없는 긴 것(주소·영문 한 덩어리)은 그러면 삐져나가므로
`overflow-wrap:break-word` 를 함께 둡니다. **둘을 같이 써야 합니다** —
`keep-all` 만 두면 긴 주소 하나에 가로 스크롤이 생깁니다.

---

## 서비스 둘러보기 쪽 (services.html) — 2026-08-30

첫 화면의 `서비스 둘러보기` 단추가 갈 곳입니다. 네 가지를 한 쪽에 모았습니다.

| | |
|---|---|
| **01 웹 서비스** | Re:Bind · Re:Call · Re:Store — 화면 한 컷씩 + `/reservice` 링크 |
| **02 앱 개발** | 앱 일곱. 스토어 등록까지 한다는 것을 앞세웠습니다 |
| **03 맞춤형 소프트웨어** | 현장에 가서 보고 만든다 — 네 단계 |
| **04 웹사이트** | 원페이지 · 반응형 · 넘긴 뒤에도 유지보수 |

앱 그림은 `python3 build-appshots.py` 로 만듭니다. 앱들은 폰에서 도는 것이라
여기서 띄워 찍을 수가 없어서, **각 프로젝트에 이미 있는 스토어 제출용 원본**을
가져다 460px 로 줄입니다. 원본 자리는 그 파일 위쪽 표에 적어 두었습니다.

스토어에 올린 **꾸민 그림**(글자·기기 테두리가 얹힌 것) 말고 실제 화면 그대로인
것을 골랐습니다 — 홈페이지에서 폰 그림을 따로 씌우기 때문에 테두리가 두 겹이 됩니다.

### ⚠ 동행만 화면이 없습니다

`~/donghaeng` 에 스토어용 화면이 없어서 **앱 마크로 대신**하고 있습니다.
`준비 중` 딱지(주황)를 달아 두었지만, 화면 한 장이 생기면
`build-appshots.py` 의 표에서 그 줄을 바꾸고 다시 돌리면 됩니다.

### 그림에는 치수를 꼭 적습니다

`width`·`height` 를 안 적으면 그림이 오기 전에 자리가 0 이라 화면이 튑니다.
`.phone img{height:auto}` 로 풀어 줘야 그 값이 CSS 높이로 먹지 않습니다.

---

## 자주 쓰는 명령

```bash
cd /Users/motodna/Desktop/network-dna

# 로컬에서 열어보기
python3 -m http.server 8791     # http://localhost:8791/network-dna.html
                                # ⚠ 서버 함수 쓰는 기능은 로컬에서 안 됩니다 (아래)

# API 키 점검 (로그인 불필요)
bash check-api-key.sh

# 서버 함수 배포 — cors.ts 나 ALLOWED_ORIGIN 을 바꾸면 둘 다 다시 배포
supabase functions deploy read-card  --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy admin-user --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy signup       --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy subscription --project-ref izrtclsqhsgkuwsffifn

# 새 회사·관리자 만들기
bash setup-admin.sh
```

⚠ **로컬(`localhost:8791`)에서는 서버 함수를 쓰는 기능이 전부 안 됩니다.**
`ALLOWED_ORIGIN` 에 localhost 가 없어서 브라우저가 응답을 버립니다 —
서버는 목록의 **첫 주소**를 돌려주는데, 그게 부른 곳과 다르면 브라우저가 막습니다.
**명함 인식 · 명함 사용량 구획 · 계정 관리**가 여기 해당합니다.

확인 명령 (200 이 떠도 `access-control-allow-origin` 이 부른 주소와 달라야 정상입니다):

```bash
curl -s -i -X OPTIONS https://izrtclsqhsgkuwsffifn.supabase.co/functions/v1/read-card \
  -H "Origin: http://localhost:8791" -H "Access-Control-Request-Method: POST" \
  | grep -i access-control-allow-origin
```

화면 모양·목록·입력은 로컬에서 보입니다. **서버를 타는 것은 `recall.dnalabs.kr` 에서 확인**하세요.
localhost 를 목록에 넣는 것은 권하지 않습니다 — Re:Bind 와 함께 쓰는 값 하나라
네 함수를 모두 다시 배포해야 하고, 운영 설정에 개발용 주소가 남습니다.


안드로이드 앱 다시 만들기:

```bash
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
export ANDROID_HOME="$HOME/Library/Android/sdk"
cd app && npm run sync && cd android && ./gradlew assembleDebug
```

PDF 만들기 (LibreOffice 없음 · Chrome 헤드리스를 씁니다):

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --no-sandbox --virtual-time-budget=12000 \
  --no-pdf-header-footer --print-to-pdf="출력.pdf" "file:///입력.html"
```

---

## 이 저장소에서 지켜야 할 것

**`git add -A` 를 쓰지 마세요.** 같은 저장소에서 **Re:Bind**(제조 공정·거래명세서,
`~/Desktop/Rebind` 로 분리됨) 작업이 병행됩니다. 2026-08-27 에 `git add -A` 로
그쪽 파일 3개를 쓸어담아 푸시한 적이 있습니다.

```bash
git status --short              # 내가 안 만진 것이 있는지 먼저 본다
git add <내가 고친 파일만>
git diff --cached --name-only   # 담긴 것을 한 번 더 확인
```

작업트리에 `bindery.html`, `supabase/functions/share-view/`,
`supabase/migrations/0002_works.sql` 의 삭제분이 커밋 안 된 채 남아 있습니다.
**Re:Bind 쪽 작업이므로 건드리지 마세요.**

⚠ **마이그레이션 파일명에 프로젝트를 붙였습니다 (2026-08-30).**
`0002_` 가 Re:Call(`0002_visibility`)과 Re:Bind(`0002_works`) 양쪽에 있어서
SQL 편집기에 엉뚱한 것을 붙여넣을 뻔했습니다. 이제 `recall_` · `rebind_` 로 갈라 둡니다.
`0001_init.sql` 만 접두어가 없는데, **두 프로젝트가 함께 쓰는 기반**이기 때문입니다
(Re:Bind 도 `current_company_id()` · `is_admin()` · `touch_updated_at()` 을 여기서 씁니다).
`0002_works.sql` 은 작업트리에서 지워진 상태라 그대로 뒀습니다 —
Re:Bind 작업을 이어갈 때 `rebind_0002_works.sql` 로 바꿔 주세요.

---

## 이번에 만든 문서

바탕화면 **「Re-Call 문서」** 폴더:

| 파일 | 용도 |
|---|---|
| `웹앱_개발_및_운영_계약서_양식.docx` | 본문 21조 + 별지 1(개발 범위) + 별지 2(운영 조건) |
| `Re-Call_개인정보_처리방침.pdf` | 앱 화면과 같은 내용. 고객사 전달용 |
| `Re-Call_국외이전_고지_동의서.pdf` | 명함 인식 국외 전송. 서명란 포함 |
| `Re-Call_도입_판정_기준.pdf` | 영업용. 업종별 가능/조건부/불가 판정 |

도입 판정 기준은 웹으로도 있습니다 —
https://claude.ai/code/artifact/83fab15c-0662-48b5-b616-624541218cb7

---

## 팔면 안 되는 업종 (요약)

**금융 · 의료 · 법률 · 심리상담 · 신용정보업 · 공공기관.**
법이 그 정보의 외부 반출을 직접 금지하므로 제품을 고쳐도 풀리지 않습니다.
무료로 줘도 마찬가지이고, 위험이 회사가 아니라 담당자 개인에게 갑니다.

**가르는 기준은 업종 이름이 아니라 거기 담기는 정보입니다.**
회사 담당자의 업무용 연락처 → 안전. 법이 반출을 막은 정보 → 금지.

자세한 것은 위 판정 기준 문서에 있습니다.
