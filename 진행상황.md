# Re:Call — 진행 상황

고객관리 솔루션 · DNA Labs

마지막 정리: 2026-08-26

---

## 링크

| | |
|---|---|
| **앱 (운영)** | https://motodna.github.io/network-dna/ |
| **저장소** | https://github.com/MotoDNA/network-dna |
| **Supabase 프로젝트** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn |
| **함수 비밀값** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions/secrets |
| **SQL 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new |
| **표 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/editor |
| **함수 로그** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions |

## 접속 정보

```
Project ref : izrtclsqhsgkuwsffifn
Project URL : https://izrtclsqhsgkuwsffifn.supabase.co
Publishable : sb_publishable_G5wx-YdgwUU630x1vKmZmQ_15BgxvEX   (공개해도 되는 값)
Region      : ap-northeast-2 (서울)

앱 로그인   : 회사코드 ACTIVA / 아이디 admin / 비밀번호는 직접 정하신 것
```

설치 암호(`SETUP_SECRET`)는 `.setup-secret` 파일에 있습니다. 깃에는 올라가지 않습니다.

---

## 지금 상태

### 다 된 것

- **데이터베이스** — 표 5개, 행 단위 접근 규칙 16개. 접근제어 시험 12가지가 실제 DB에서 통과
- **서버 함수 2개 배포** — `admin-user`(계정 관리), `read-card`(명함 인식 대행)
- **보안 게이트 확인** — 로그인 없이 호출, 설치 암호 없이 회사 개설, 틀린 암호, 로그인 없이 계정 생성 모두 차단
- **회사와 관리자 계정** — ACTIVA / admin
- **앱을 Supabase 로 이전** — 로그인 화면, 세션 관리, 동기화, 로그아웃 시 기기 데이터 삭제
- **왕복 검증 통과** — 고객 생성 → 서버 저장 → 로컬 전체 삭제 → 서버에서 복원
- **명함 자동 크롭** — 정상 동작
- **`ANTHROPIC_API_KEY` — 해결됐습니다.** 지난번에 넣은 키가 실제로는 멀쩡했습니다.
  108자, `sk-ant-` 로 시작, Anthropic 이 200 으로 응답합니다.
  이제 `bash check-api-key.sh` 로 언제든 확인할 수 있습니다 (앱에 로그인하지 않아도 됩니다).
- **명함 사진 서버 업로드 — 만들었습니다.** 아래 참고.
- **이름을 Re:Call 로 바꿨습니다.** 상단 바와 로그인 화면에 DNA Labs 로고가 들어갑니다.
  로고는 파일 안에 박아 넣어서 따로 챙길 것이 없습니다. 홈 화면에 추가할 때 아이콘으로도 쓰입니다.
- **직원 계정 추가 — 만들었습니다.** 버튼만 있고 눌러도 아무 일이 없었습니다.
  이제 관리자에게는 팀원 줄이 눌리고, 비밀번호 재발급·사용 중지·재개까지 됩니다.
- **연락처 저장이 폰에서 바로 뜹니다.** 아래 참고.
- **그룹별 담당자 일괄 지정** — 설정 → 고객 그룹 관리에서 그룹 옆 사람 아이콘.
  그 그룹에 든 고객 전체의 담당자를 한 번에 바꾸거나 공유할 수 있습니다.
- **샘플 데이터 넣기 버튼이 터지던 것 고쳤습니다.** `const已` 오타로 눌리면 오류가 났습니다.
- **`ALLOWED_ORIGIN` 잠갔습니다.** 이제 아래 두 곳에서만 서버 함수를 부를 수 있습니다.
  ```
  https://motodna.github.io      운영
  http://localhost:8791          로컬 테스트
  ```
  다른 사이트에서 부르면 브라우저가 막습니다. 확인했습니다.
  전에는 한 곳만 적을 수 있어서 잠그면 로컬 테스트가 막혔는데, 쉼표로 여러 곳을 적을 수 있게 고쳤습니다.
  테스트가 끝나면 `localhost` 를 빼서 완전히 잠그면 됩니다.

### 명함 사진이 오가는 방식

사진은 이 기기와 서버 양쪽에 둡니다. 경로는 `{회사id}/{고객id}.jpg` 이고,
폴더 이름이 곧 접근 권한이라 다른 회사 사진에는 닿지 않습니다.

| 언제 | 무슨 일이 | 
|---|---|
| 사진을 찍거나 바꾸면 | 표시만 해 두고, 다음 동기화에 올라갑니다 |
| 동기화할 때 | 사진을 **먼저** 올린 뒤 고객 정보를 올립니다 (경로를 같이 실어야 하므로) |
| 올리다 실패하면 | 대기 상태로 남아 다음 동기화에서 다시 시도합니다 |
| 다른 기기에서 받을 때 | 동기화 뒤 뒤에서 천천히 받아옵니다 (한 번에 8장) |
| 상세 화면을 열었는데 아직 안 받았으면 | 그 자리에서 받아서 다시 그립니다 |
| 고객을 지우면 | 서버의 사진도 지우고, 이 기기에서도 비웁니다 |

한 번에 5장씩만 올려 화면이 오래 멈추지 않게 했습니다. 나머지는 20초 뒤 자동으로 이어집니다.
이 기능이 생기기 전에 만든 고객 사진도 처음 동기화할 때 자동으로 올라갑니다.

명함 한 장이 대략 200~400KB 입니다. Free 요금제 보관함이 1GB 이니 3천 장쯤 들어갑니다.

### 연락처 저장 — 폰별로 다릅니다

명함을 저장한 뒤 **연락처** 버튼을 누르면:

| | 무슨 일이 | 확인 |
|---|---|---|
| **아이폰** | 정보가 다 채워진 연락처 화면이 바로 뜹니다. **"새로운 연락처 등록"** 을 누르면 끝. 닫으면 앱 그대로 | iPhone 17 Pro · iOS 26 사파리에서 확인 |
| **안드로이드** | 공유 시트가 뜹니다. 거기서 연락처를 고르면 저장됩니다 | Pixel 8 · Android 15 |

아이폰은 보이지 않는 틀(iframe)에 명함을 실어 띄웁니다. 주소창을 옮기지 않으므로
**홈 화면에 추가해서 써도 앱이 밀려나지 않습니다.** 이게 핵심이라 이 방식을 골랐습니다.

안드로이드에서 연락처 편집 화면을 곧장 여는 방법(`intent://`)도 시험해 봤지만
크롬이 막습니다 — 눌러도 아무 일이 일어나지 않습니다. 그래서 공유 시트를 씁니다.

### 아직 안 한 것

| 할 일 | 설명 |
|---|---|
| **로그인이 필요한 기능 실기 확인** | 명함 사진 업로드·직원 계정 추가·그룹 담당자 지정. 코드와 화면은 확인했지만 실제 왕복은 아직. 아래 "확인해 주실 것" 참고 |
| 검증용 데이터 정리 | 앱에 "검증고객 / 검증컴퍼니" 가 남아 있음. 앱에서 지우면 서버에서도 지워집니다 |
| 관리자 이름 | "리자" 로 잘려 들어감. 앱 → 설정 → 내 계정 → 이름 눌러서 수정 |
| 2단계 인증 | Supabase 가 지원. 나중에 켤 수 있음 |
| 백업 | Free 요금제는 자동 백업 없음. 유료 전환 시 매일 백업 |
| 개인정보 처리방침 | 고객 이름·전화·이메일을 다루므로 필요 |

---

## 확인해 주실 것

로그인이 필요한 부분은 대신 해 드릴 수 없어 남겨 둡니다. 순서대로 해보시면 됩니다.

1. **명함 인식** — 앱에서 명함을 촬영합니다. 회사·이름·전화가 자동으로 채워지면 API 키 문제는 끝난 것입니다.
2. **사진이 서버로 가는지** — 저장한 뒤 우측 상단 표시가 `동기화됨` 이 되면
   [보관함](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/storage/buckets/cards)에
   `{회사id}/{고객id}.jpg` 가 보여야 합니다.
3. **사진이 돌아오는지** — 다른 기기(또는 시크릿 창)에서 같은 계정으로 들어가
   그 고객을 열면 명함 사진이 잠깐 뒤에 나타나야 합니다.
4. **연락처 버튼** — 고객 상세에서 **연락처**. 아이폰이면 연락처 화면이 바로 떠야 합니다.
5. **직원 계정 추가** — 설정 → 팀원 → **직원 계정 추가**. 만든 뒤 그 아이디로 들어가지는지.
   만든 팀원 줄을 누르면 비밀번호 재발급·사용 중지도 됩니다.
6. **그룹 담당자** — 설정 → 고객 그룹 관리 → 그룹 옆 사람 아이콘.
7. **샘플 데이터** — 설정 → 데이터 → 샘플 데이터 넣기 (전에는 눌러도 안 됐습니다)
8. **관리자 이름 고치기** — 설정 → 내 계정 → 이름 (지금 "리자" 로 잘려 있습니다)
9. **검증용 데이터 지우기** — 목록에서 "검증고객 / 검증컴퍼니" 삭제

잘 안 되면 [함수 로그](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions)와
브라우저 콘솔을 함께 보시면 원인이 나옵니다.

---

## 자주 쓰는 명령

모두 `/Users/motodna/Desktop/network-dna` 에서 실행합니다.

```bash
cd /Users/motodna/Desktop/network-dna

# API 키가 제대로 들어갔는지 확인 (로그인 불필요)
bash check-api-key.sh

# API 키 넣기
bash setup-api-key.sh

# 새 회사와 관리자 만들기
bash setup-admin.sh

# 서버 함수 다시 배포
supabase functions deploy read-card  --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy admin-user --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn

# 비밀값 목록 보기 (값은 안 보이고 지문만 나옵니다)
supabase secrets list --project-ref izrtclsqhsgkuwsffifn
```

테스트가 끝나면 `localhost` 를 빼서 완전히 잠급니다.
값을 바꾼 뒤에는 두 함수를 **다시 배포해야** 반영됩니다.

```bash
supabase secrets set ALLOWED_ORIGIN="https://motodna.github.io" --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy read-card  --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy admin-user --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

```bash
# 로컬에서 테스트할 때 (앱을 http://localhost:8791 로 띄웁니다)
python3 -m http.server 8791
```

> 셸 주의: 맥 기본 셸은 zsh 입니다. `read -p` 는 bash 문법이라 zsh 에서 안 됩니다.
> 위 `.sh` 파일들은 `#!/bin/bash` 로 되어 있고 `bash` 로 실행하므로 괜찮습니다.
> 여러 줄 명령은 붙여넣지 말고 스크립트를 쓰세요.
> 한글 파일명도 자소 분리 때문에 붙여넣기가 안 될 수 있어 영문으로 두었습니다.

## 파일 구성

```
network-dna.html              앱 본체 (이 파일 하나로 동작)
supabase/
  migrations/0001_init.sql    표 · 접근 규칙 · 권한 · 명함 보관함
  tests/rls-test.sql          접근제어 시험 12가지
  functions/admin-user/       계정 관리 (service_role 을 쓰는 유일한 곳)
  functions/read-card/        명함 인식 대행 (API 키가 여기에만 있음) · 설치 점검
  functions/_shared/cors.ts   어디서 부를 수 있는지
  project.json                접속 주소와 공개 키
setup-admin.sh                회사·관리자 계정 만들기
setup-api-key.sh              API 키 넣기
check-api-key.sh              API 키가 제대로 들어갔는지 확인
백엔드-설치방법.md              설치 절차 (다른 고객사에도 이대로)
NETWORK-DNA-IR.pptx           IR 자료 (깃 제외)
```
