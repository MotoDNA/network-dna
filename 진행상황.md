# Re:Call — 진행 상황

고객관리 솔루션 · DNA Labs

마지막 정리: 2026-08-26

---

## 링크

| | |
|---|---|
| **앱 (운영)** | https://recall.dnalabs.kr |
| **앱 (예전 주소)** | https://motodna.github.io/network-dna/ — 새 주소로 자동 전달됩니다 |
| **안드로이드 앱 설치** | https://recall-install.vercel.app |
| **저장소** | https://github.com/MotoDNA/network-dna |
| **Supabase 프로젝트** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn |
| **함수 비밀값** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions/secrets |
| **SQL 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new |
| **표 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/editor |
| **함수 로그** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions |

## 접속 정보

```
Project ref : izrtclsqhsgkuwsffifn
Project URL : https://izrtclsqhsgkuwsffifn.supabase.co
Publishable : sb_publishable_G5wx-YdgwUU630x1vKmZmQ_15BgxvEX   (공개해도 되는 값)
Region      : ap-northeast-2 (서울)

앱 로그인   : 회사코드 ACTIVA / 아이디 admin / 비밀번호는 직접 정하신 것
```

설치 암호(`SETUP_SECRET`)는 `.setup-secret` 파일에 있습니다. 깃에는 올라가지 않습니다.

---

## 지금 상태

### 다 된 것

- **데이터베이스** — 표 5개, 행 단위 접근 규칙 16개. 접근제어 시험 12가지가 실제 DB에서 통과
- **서버 함수 2개 배포** — `admin-user`(계정 관리), `read-card`(명함 인식 대행)
- **보안 게이트 확인** — 로그인 없이 호출, 설치 암호 없이 회사 개설, 틀린 암호, 로그인 없이 계정 생성 모두 차단
- **회사와 관리자 계정** — ACTIVA / admin
- **앱을 Supabase 로 이전** — 로그인 화면, 세션 관리, 동기화, 로그아웃 시 기기 데이터 삭제
- **왕복 검증 통과** — 고객 생성 → 서버 저장 → 로컬 전체 삭제 → 서버에서 복원
- **명함 자동 크롭** — 정상 동작
- **`ANTHROPIC_API_KEY` — 해결됐습니다.** 지난번에 넣은 키가 실제로는 멀쩡했습니다.
  108자, `sk-ant-` 로 시작, Anthropic 이 200 으로 응답합니다.
  이제 `bash check-api-key.sh` 로 언제든 확인할 수 있습니다 (앱에 로그인하지 않아도 됩니다).
- **명함 사진 서버 업로드 — 만들었습니다.** 아래 참고.
- **이름을 Re:Call 로 바꿨습니다.** 상단 바와 로그인 화면에 DNA Labs 로고가 들어갑니다.
  로고는 파일 안에 박아 넣어서 따로 챙길 것이 없습니다. 홈 화면에 추가할 때 아이콘으로도 쓰입니다.
- **직원 계정 추가 — 만들었습니다.** 버튼만 있고 눌러도 아무 일이 없었습니다.
  이제 관리자에게는 팀원 줄이 눌리고, 비밀번호 재발급·사용 중지·재개까지 됩니다.
- **연락처 저장이 폰에서 바로 뜹니다.** 아래 참고.
- **그룹별 담당자 일괄 지정** — 설정 → 고객 그룹 관리에서 그룹 옆 사람 아이콘.
  그 그룹에 든 고객 전체의 담당자를 한 번에 바꾸거나 공유할 수 있습니다.
- **샘플 데이터 넣기 버튼이 터지던 것 고쳤습니다.** `const已` 오타로 눌리면 오류가 났습니다.
- **`ALLOWED_ORIGIN` 잠갔습니다.** 이제 아래 두 곳에서만 서버 함수를 부를 수 있습니다.
  ```
  https://recall.dnalabs.kr      운영
  https://motodna.github.io      예전 주소 (당분간 함께 열어 둡니다)
  http://localhost:8791          로컬 테스트
  ```
  다른 사이트에서 부르면 브라우저가 막습니다. 확인했습니다.
  전에는 한 곳만 적을 수 있어서 잠그면 로컬 테스트가 막혔는데, 쉼표로 여러 곳을 적을 수 있게 고쳤습니다.
  테스트가 끝나면 `localhost` 를 빼서 완전히 잠그면 됩니다.

### 명함 사진이 오가는 방식

사진은 이 기기와 서버 양쪽에 둡니다. 경로는 `{회사id}/{고객id}.jpg` 이고,
폴더 이름이 곧 접근 권한이라 다른 회사 사진에는 닿지 않습니다.

| 언제 | 무슨 일이 | 
|---|---|
| 사진을 찍거나 바꾸면 | 표시만 해 두고, 다음 동기화에 올라갑니다 |
| 동기화할 때 | 사진을 **먼저** 올린 뒤 고객 정보를 올립니다 (경로를 같이 실어야 하므로) |
| 올리다 실패하면 | 대기 상태로 남아 다음 동기화에서 다시 시도합니다 |
| 다른 기기에서 받을 때 | 동기화 뒤 뒤에서 천천히 받아옵니다 (한 번에 8장) |
| 상세 화면을 열었는데 아직 안 받았으면 | 그 자리에서 받아서 다시 그립니다 |
| 고객을 지우면 | 서버의 사진도 지우고, 이 기기에서도 비웁니다 |

한 번에 5장씩만 올려 화면이 오래 멈추지 않게 했습니다. 나머지는 20초 뒤 자동으로 이어집니다.
이 기능이 생기기 전에 만든 고객 사진도 처음 동기화할 때 자동으로 올라갑니다.

명함 한 장이 대략 200~400KB 입니다. Free 요금제 보관함이 1GB 이니 3천 장쯤 들어갑니다.

### 연락처 저장 — 폰별로 다릅니다

명함을 저장한 뒤 **연락처** 버튼을 누르면:

| | 무슨 일이 | 상태 |
|---|---|---|
| **아이폰** | 정보가 다 채워진 연락처 화면이 바로 뜹니다. **"새로운 연락처 등록"** 을 누르면 끝. 닫으면 앱 그대로 | 확인됨 (iOS 26 사파리) |
| **안드로이드** | 공유 시트 → 연락처, 또는 파일을 받아 **열기** → 가져오기 화면 | 가져오기 화면까지가 한계 |

아이폰은 보이지 않는 틀(iframe)에 명함을 실어 띄웁니다. 주소창을 옮기지 않으므로
**홈 화면에 추가해서 써도 앱이 밀려나지 않습니다.** 이게 핵심이라 이 방식을 골랐습니다.

**안드로이드는 "새 연락처" 편집 화면을 웹에서 열 수 없습니다.** 두 가지로 확인했습니다.

- `intent://` 로 연락처 편집기를 여는 방법 — 크롬이 막습니다. 눌러도 아무 일이 없습니다.
- vcf 파일을 열면 안드로이드가 **가져오기(ImportVCard) 화면**을 띄웁니다. 편집기가 아닙니다.
  이건 안드로이드가 vCard 를 다루는 기본 방식이라 앱에서 바꿀 수 없습니다.

대신 공유 시트가 제대로 뜨도록 고쳤습니다. 전에는 형식을 바꿔 가며 세 번 시도했는데,
공유 시트는 "방금 눌렀음" 표시가 살아 있을 때만 열리고 그 표시는 첫 호출에서 사라집니다.
그래서 2·3번째는 무조건 실패하고 파일 내려받기로 떨어졌습니다. 이제 한 번만 시도합니다.

### 아직 안 한 것

| 할 일 | 설명 |
|---|---|
| **로그인이 필요한 기능 실기 확인** | 명함 사진 업로드·직원 계정 추가·그룹 담당자 지정. 코드와 화면은 확인했지만 실제 왕복은 아직. 아래 "확인해 주실 것" 참고 |
| 검증용 데이터 정리 | 앱에 "검증고객 / 검증컴퍼니" 가 남아 있음. 앱에서 지우면 서버에서도 지워집니다 |
| 관리자 이름 | "리자" 로 잘려 들어감. 앱 → 설정 → 내 계정 → 이름 눌러서 수정 |
| 2단계 인증 | Supabase 가 지원. 나중에 켤 수 있음 |
| 백업 | Free 요금제는 자동 백업 없음. 유료 전환 시 매일 백업 |
| 개인정보 처리방침 | 고객 이름·전화·이메일을 다루므로 필요 |

---

## 확인해 주실 것

로그인이 필요한 부분은 대신 해 드릴 수 없어 남겨 둡니다. 순서대로 해보시면 됩니다.

1. ~~명함 인식~~ — 확인됨
2. ~~사진이 서버로 가는지~~ — 확인됨
3. ~~사진이 돌아오는지~~ — 시크릿 창에서 명함이 보이는 것으로 확인됨
4. **연락처 버튼** — 안드로이드는 위 설명대로 가져오기 화면까지가 한계입니다. 아이폰에서 확인 부탁드립니다
5. ~~직원 계정 추가~~ — 확인됨
6. ~~그룹 담당자~~ — 확인됨
7. ~~샘플 데이터~~ — 확인됨
8. ~~관리자 이름~~ — 확인됨
9. ~~검증용 데이터 정리~~ — 확인됨

### 이번에 더한 것

- **명함 사진 저장 버튼** — 고객 상세 → 명함 → 사진 저장
- **안드로이드 앱** — 주소록에 바로 저장 (아래 참고)
- **팀 현황판** — 새 탭. 아래 참고

### 팀 현황판

탭이 하나 늘었습니다. **지금까지 모든 것이 "고객 한 명 안에" 갇혀 있었는데**,
이걸 팀 단위로 다시 묶어 보여 줍니다.

| | |
|---|---|
| **내 할 일** | 고객을 가로질러 모읍니다. 마감 순으로 서고 지난 것은 빨갛게 표시됩니다. 여기서 바로 체크해 끝낼 수 있습니다 |
| **팀이 한 일** | 오늘·이번 주·이번 달. 누가 어느 고객에게 무엇을 남겼는지. 누르면 그 고객으로 갑니다 |
| **팀원별** | 고른 기간 동안 누가 얼마나 움직였는지 막대로 |

**새로 저장하는 것은 없습니다.** 표도 안 늘었고 서버도 손대지 않았습니다.
이미 쌓이고 있던 기록을 다르게 볼 뿐이라, 어제까지의 데이터가 그대로 나옵니다.

보이는 범위는 데이터베이스가 정합니다. 내가 볼 수 없는 고객의 기록은 애초에
기기에 내려와 있지 않으니 여기서 따로 거를 것이 없습니다.
관리자는 회사 전체가, 직원은 자기 담당과 공유받은 것만 보입니다.

**내 할 일**의 기준은 *내가 담당인 고객의 할 일* + *내가 적은 할 일* 입니다.

### 도메인 — recall.dnalabs.kr (2026-08-27 전환 완료)

가비아에서 산 `dnalabs.kr` 의 하위 주소를 웹앱에 붙였습니다.

```
가비아 DNS    CNAME   recall  →  motodna.github.io.
GitHub Pages  사용자 지정 도메인 recall.dnalabs.kr (저장소의 CNAME 파일)
인증서        Let's Encrypt · GitHub 가 자동 발급하고 자동 갱신합니다
```

**예전 주소(`motodna.github.io/network-dna/`)도 그대로 열립니다.** GitHub 가 새 주소로
자동 전달하므로 직원분들이 즐겨찾기를 고치지 않아도 됩니다.

안드로이드 앱은 파일을 앱 안에 담고 있어 **영향받지 않습니다.** 다시 빌드하지 않아도 됩니다.

`ALLOWED_ORIGIN` 에는 새 주소와 예전 주소를 함께 넣어 두었습니다. 예전 주소를 쓰는 사람이
없어지면 그때 빼면 됩니다.

### 고객별 공개 범위  ⚠ SQL 실행이 필요합니다

고객마다 누가 볼 수 있는지 셋 중에 고릅니다. **고객 상세 → 공개 범위**

| | 누가 보나 |
|---|---|
| **공개** | 회사 사람 모두 |
| **선택공개** | 담당자 + 공유받은 사람 + 관리자 — **지금까지의 방식** |
| **비공개** | 담당자 본인만. 관리자도 내용은 못 봅니다 |

비공개로 바꾸면 공유 목록은 비워집니다. 의도와 데이터가 어긋나지 않게 하려고요.

**관리자는 비공개 고객의 내용을 볼 수 없지만, 팀 현황판에서
"이 담당자가 비공개 3건을 갖고 있다"는 숫자는 봅니다.** 있다는 사실은 알되
안은 못 보는 상태입니다.

그리고 팀 현황판의 숫자가 **진짜 숫자**가 됐습니다. 전에는 내가 볼 수 있는 것만
세어서, 직원 화면에서는 남의 활동량이 실제보다 적게 나왔습니다. 이제 서버가
회사 전체를 세어 주고 내용은 한 줄도 내보내지 않습니다.
"팀이 한 일"에는 `10건 중 1건 보임` 처럼 안 보이는 게 있다는 것도 적힙니다.

#### 해야 할 일 — SQL 한 번 실행

[SQL 편집기](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new)에
`supabase/migrations/0002_visibility.sql` 내용을 붙여넣고 실행하시면 됩니다.

**지금 있는 고객은 전부 "선택공개"가 되어 오늘과 똑같이 동작합니다.**
보이던 것이 사라지지 않습니다.

실행하기 전에도 앱은 그냥 돌아갑니다. 서버에 칸이 없으면 공개 범위 줄이
조용히 숨고 예전처럼 동작합니다. 그래서 순서를 신경 쓰지 않으셔도 됩니다.

### 다음에 할 만한 것 — 알림·멘션

현황판을 며칠 써 보신 뒤에 정하는 게 좋겠습니다.
기록에 `@이지훈` 을 쓰면 그 사람에게 알림이 가고, 고객을 공유받거나 할 일 마감이
다가올 때도 알리는 것입니다. 표 하나(`notifications`)와 접근 규칙이 늘고 1~2일 걸립니다.

사내 채팅이나 문서(Slack·Notion 같은 것)는 만들지 않기로 했습니다. 그건 CRM 에
기능을 더하는 게 아니라 제품을 하나 더 만드는 일이고, 파일 하나로 굴러가는
지금 구조도 못 버팁니다. 필요해지면 쓰시는 메신저로 알림만 보내는 쪽이 낫습니다.

### 이번에 고친 것 (다시 봐 주세요)

- **팝업 열린 채 스크롤하면 배경이 밀리던 것** — 시트가 열려도 본문 스크롤이 안 잠겨 있었습니다
- **명함을 다시 찍어도 앞 정보가 그대로 남던 것** — 빈 칸에만 넣도록 되어 있었습니다.
  이제 앞 명함이 채운 칸은 새 값으로 바뀌고, 직접 고친 칸은 그대로 둡니다
- **비밀번호 입력 안내** — 직원 추가·재발급 칸 아래에 규칙이 실시간으로 뜹니다
- **연락처 공유 시트** — 위 설명대로 한 번만 시도하도록 고쳤습니다

### 명함 사진을 폰에 저장하기

고객 상세 → **명함** 오른쪽의 **사진 저장** 버튼을 쓰면 됩니다.
누르면 공유 시트가 뜨고 **이미지 저장**을 고르면 사진 앱에 들어갑니다.
파일 이름은 `홍길동-명함.jpg` 처럼 고객 이름이 붙습니다.
(iOS 26 시뮬레이터에서 사진 보관함에 실제로 들어가는 것까지 확인)

찍는 즉시 갤러리에 자동으로 들어가지는 않습니다. 웹앱은 갤러리에 직접 쓸 수 없습니다.
사진 자체는 이 기기와 서버 양쪽에 있으니 잃어버릴 걱정은 없습니다.

### 안드로이드 앱 — 주소록에 바로 저장

**만들었습니다.** [Capacitor](https://capacitorjs.com) 로 감싸고
[@capacitor-community/contacts](https://github.com/capacitor-community/contacts)(무료·MIT)를 썼습니다.
앱에서 명함의 **연락처 저장**을 누르면 **화면 하나 없이 곧장 주소록에 들어갑니다.**
공유 시트도, 가져오기 화면도 거치지 않습니다.

Pixel 8 · Android 15 에뮬레이터에서 확인한 것:

```
이름   홍길동
회사   테스트상사 · 영업부장
전화   010-1234-5678   (휴대전화 · 기본)
이메일 hong@test.co.kr (직장 · 기본)
주소   서울시 강남구 테헤란로 1 (직장)
메모   [그룹] 주요 고객
사진   명함 사진이 연락처 사진으로 붙습니다
```

**웹과 앱이 같은 파일 하나입니다.** `network-dna.html` 이 실행될 때
`window.Capacitor` 가 있는지 보고 앱인지 웹인지 스스로 가립니다.
앱 전용 코드를 따로 두지 않았으니 앞으로도 그 파일만 고치면 됩니다.

```
network-dna.html   ← 여기만 고칩니다
      │
      ├─ 깃허브 페이지 → 브라우저 (아이폰은 연락처 화면, 안드로이드는 공유 시트)
      └─ app/npm run sync → 앱   (주소록에 바로 저장)
```

만드는 방법은 [app/README.md](app/README.md) 에 적어 두었습니다. 요약하면:

```bash
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
export ANDROID_HOME="$HOME/Library/Android/sdk"
cd app && npm install && npm run sync
cd android && ./gradlew assembleDebug
```

**앱은 자동으로 갱신되지 않습니다.** 웹을 고치면 깃허브에 올리는 것으로 끝이지만,
앱은 위 과정을 다시 거쳐 새 APK 를 설치해야 합니다. 이게 앱으로 감싼 대가입니다.
그래서 웹을 계속 주로 쓰시고, 앱은 연락처를 많이 저장하는 분에게만 주는 쪽을 권합니다.

플레이스토어에 올리려면 서명한 release 빌드와 개발자 등록(1회 25달러), 심사가 필요합니다.
`appId` 는 `com.dnalabs.recall` 로 두었는데 등록 뒤에는 못 바꾸니 올리기 전에 확정해 주세요.

아이폰은 웹으로 열어도 연락처 화면이 바로 뜨므로 앱이 급하지 않습니다.
필요하면 `npx cap add ios` 로 추가할 수 있습니다 (Xcode 와 애플 개발자 등록 연 99달러 필요).

잘 안 되면 [함수 로그](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions)와
브라우저 콘솔을 함께 보시면 원인이 나옵니다.

---

## 자주 쓰는 명령

모두 `/Users/motodna/Desktop/network-dna` 에서 실행합니다.

```bash
cd /Users/motodna/Desktop/network-dna

# API 키가 제대로 들어갔는지 확인 (로그인 불필요)
bash check-api-key.sh

# API 키 넣기
bash setup-api-key.sh

# 새 회사와 관리자 만들기
bash setup-admin.sh

# 서버 함수 다시 배포
supabase functions deploy read-card  --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy admin-user --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn

# 비밀값 목록 보기 (값은 안 보이고 지문만 나옵니다)
supabase secrets list --project-ref izrtclsqhsgkuwsffifn
```

테스트가 끝나면 `localhost` 를 빼서 완전히 잠급니다.
값을 바꾼 뒤에는 두 함수를 **다시 배포해야** 반영됩니다.

```bash
supabase secrets set ALLOWED_ORIGIN="https://recall.dnalabs.kr" --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy read-card  --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy admin-user --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

```bash
# 로컬에서 테스트할 때 (앱을 http://localhost:8791 로 띄웁니다)
python3 -m http.server 8791
```

> 셸 주의: 맥 기본 셸은 zsh 입니다. `read -p` 는 bash 문법이라 zsh 에서 안 됩니다.
> 위 `.sh` 파일들은 `#!/bin/bash` 로 되어 있고 `bash` 로 실행하므로 괜찮습니다.
> 여러 줄 명령은 붙여넣지 말고 스크립트를 쓰세요.
> 한글 파일명도 자소 분리 때문에 붙여넣기가 안 될 수 있어 영문으로 두었습니다.

## 파일 구성

```
network-dna.html              앱 본체 (이 파일 하나로 동작)
supabase/
  migrations/0001_init.sql    표 · 접근 규칙 · 권한 · 명함 보관함
  migrations/0002_visibility.sql  고객별 공개 범위 · 팀 집계 (실행 필요)
  tests/rls-test.sql          접근제어 시험 12가지
  functions/admin-user/       계정 관리 (service_role 을 쓰는 유일한 곳)
  functions/read-card/        명함 인식 대행 (API 키가 여기에만 있음) · 설치 점검
  functions/_shared/cors.ts   어디서 부를 수 있는지
  project.json                접속 주소와 공개 키
app/
  sync-web.js                 network-dna.html 을 앱에 담습니다
  capacitor.config.json       앱 이름·식별자
  android/                    안드로이드 프로젝트
  README.md                   앱 만드는 방법
setup-admin.sh                회사·관리자 계정 만들기
setup-api-key.sh              API 키 넣기
check-api-key.sh              API 키가 제대로 들어갔는지 확인
백엔드-설치방법.md              설치 절차 (다른 고객사에도 이대로)
NETWORK-DNA-IR.pptx           IR 자료 (깃 제외)
```


---

## Re:Bind 가 따로 나갔습니다 (2026-08-27)

제조 공정과 거래명세서를 다루는 **Re:Bind** 는 `~/Desktop/Rebind` 로 옮겼습니다.
Re:Call 과 **같은 Supabase 프로젝트, 같은 계정**을 쓰지만 폴더와 파일은 나뉩니다.

이 저장소에서 아래 셋이 지워진 것은 그리로 옮겨 갔기 때문입니다.

```
bindery.html
supabase/functions/share-view/
supabase/migrations/0002_works.sql   → Rebind/sql/0003_works.sql
```

`network-dna.html` 과 Re:Call 의 마크는 **그대로입니다.** 손대지 않았습니다.
